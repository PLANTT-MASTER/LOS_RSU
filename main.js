// This file contains JavaScript code for handling interactive elements of the webpage.

document.addEventListener('DOMContentLoaded', () => {
    // Example: Add event listeners for buttons or other interactive elements
    const awarenessButton = document.getElementById('awareness-button');
    if (awarenessButton) {
        awarenessButton.addEventListener('click', () => {
            alert('¡Gracias por interesarte en la responsabilidad social y la gestión de residuos!');
        });
    }

    // Additional interactive features can be added here
});